GEMINI_API_KEY="your_gemini_api_key"
OPENWEATHER_API_KEY="your_openweather_api_key"
MONGODB_URI="mongodb://localhost:27017/stylemind"
JWT_SECRET="your_jwt_secret"
GOOGLE_CLIENT_ID="your_google_client_id"
# Optional: extra OAuth client IDs (e.g. Android/iOS native clients) whose tokens
# are accepted, comma-separated. GOOGLE_CLIENT_ID is always allowed.
GOOGLE_ALLOWED_AUDIENCES="your_android_client_id,your_ios_client_id"
# Optional: transactional email for password reset codes (https://resend.com).
# Without a key, reset codes are logged to the server console (dev only).
RESEND_API_KEY="your_resend_api_key"
EMAIL_FROM="FitPick <noreply@yourdomain.com>"
PORT="8787"
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173,http://localhost:8081"
# Optional: New Relic monitoring. When set, audit-trail entries and errors are
# mirrored to New Relic. For full APM (request/DB tracing) start the server
# with `npm run start:apm` and set these as real host env vars (not only in
# .env.production — the APM agent boots before dotenv runs).
NEW_RELIC_LICENSE_KEY="your_new_relic_license_key"
NEW_RELIC_APP_NAME="fitpick-api"
# Optional: audit trail retention in MongoDB (days, default 90).
AUDIT_LOG_RETENTION_DAYS="90"
